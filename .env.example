VITE_CHAIN_ID=11142220
VITE_CELO_SEPOLIA_RPC_URL=https://forno.celo-sepolia.celo-testnet.org
VITE_CELO_RPC_URL=https://forno.celo.org

# Optional — only needed if you want to support WalletConnect (non-MiniPay wallets).
VITE_WALLETCONNECT_PROJECT_ID=

# Goldsky subgraph URL. When set, dashboard uses GraphQL instead of getLogs.
VITE_SUBGRAPH_URL=

# Backend (Hono) base URL. When set, AI copilot + parse-invoice + PDF download unlock.
# E.g. http://localhost:3001 (dev) or https://paylink-backend.vercel.app (prod).
VITE_BACKEND_URL=
